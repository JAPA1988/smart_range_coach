name: smart_range_coach
description: Smart Range Coach – camera smoke test with MoveNet POC
version: 1.0.0+1
publish_to: 'none'
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.11.0
  camera_windows: ^0.2.6+4
  path_provider: ^2.1.0
  path: ^1.9.0
  video_player: ^2.9.1  # Optimiert für bessere Performance
  video_player_win: ^3.0.0
  tflite_flutter: ^0.12.1
  # ffmpeg_kit_flutter: ^6.0.3  # Discontinued - nicht benötigt
  image: ^3.3.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/movenet_singlepose_lightning.tflite
    - assets/pro_swings/sample_pro.json